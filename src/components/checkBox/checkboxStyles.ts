import { StyleSheet } from "react-native";

export const checkBoxStyles = StyleSheet.create({
    container:{
        flexDirection: 'row',
        alignItems: 'center',
    },
    label:{
        marginRight: 5
    }
})